@model WebApplication1.Models.TicketViewModel
@{
    ViewData["Title"] = "Tour Ticket";
    Layout = "_Layout";
}

<link rel="stylesheet" href="~/css/whatson.css" />

<!-- Back Button -->
<section class="booking-header">
    <a href="/WhatsOn" class="back-button">← Back to What's On</a>
</section>

<!-- Ticket Section -->
<section class="ticket-section">
    <div class="ticket-container">

        <!-- Ticket Box -->
        <div class="ticket-box">
            <div class="ticket-header">
                <h1 class="ticket-label">TICKET</h1>
            </div>
            <div class="ticket-info">
                <div class="ticket-row">
                    <span class="ticket-field-name">@Model.Title.ToUpper() - @Model.Quantity TICKET(S)</span>
                </div>
            </div>
            <div class="ticket-number-section">
                <p class="ticket-number-label">NUM:</p>
                <p class="ticket-number">@Model.TicketCode</p>
            </div>
            <div class="ticket-footer">
                <div class="ticket-row">
                    <span class="ticket-field-name">
                        @Model.Date
                        @if (!string.IsNullOrEmpty(Model.BookingTime))
                        {
                            <text>- @Model.BookingTime</text>
                            ;
                        }
                    </span>
                </div>
                <p class="ticket-message">A COPY OF THIS TICKET HAS BEEN</p>
                <p class="ticket-message">EMAILED TO YOU</p>
                <p class="ticket-message">MEMBERS CAN VIEW THIS</p>
                <p class="ticket-message">TICKET ON THEIR ACCOUNT</p>
            </div>
        </div>

        <!-- Booking Summary -->
        <div class="booking-summary">
            <h3>BOOKING CONFIRMATION</h3>
            <div class="summary-item">
                <span class="summary-label">Tour:</span>
                <span class="summary-value">@Model.Title</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Date:</span>
                <span class="summary-value">@Model.Date</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Time:</span>
                <span class="summary-value">@Model.BookingTime</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Quantity:</span>
                <span class="summary-value">@Model.Quantity</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Email:</span>
                <span class="summary-value">@Model.Email</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Cardholder Name:</span>
                <span class="summary-value">@Model.CardholderName</span>
            </div>
            <div class="summary-item">
                <span class="summary-label">Total Price:</span>
                <span class="summary-value">£@Model.TotalPrice.ToString("F2")</span>
            </div>
            <a href="/WhatsOn/Tours" class="return-link">← Return to Tours</a>
        </div>
    </div>
</section>